{
  "dependencies": {
    "concurrently": "^9.1.0",
    "dotenv": "^16.4.7",
    "express-generator": "^4.2.0"
  },
  "scripts": {
    "build": "echo 'No build step needed for the backend'",
    "start": "npx concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npx serve -s dist"
  }
}
